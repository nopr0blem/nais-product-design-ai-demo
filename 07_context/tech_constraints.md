# Technical Constraints — NAIS Platform

- Shared component library across countries
- Frontend must reuse existing widgets when possible
- Backend APIs not extensible short term
- Performance and stability prioritized over new features

Rule:
Do not propose solutions requiring new core services unless explicitly stated.
